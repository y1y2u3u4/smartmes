# SmartMES Lite - Docker配置文件总结

本文档列出了项目中所有与Docker相关的配置文件及其用途。

## 文件清单

### 核心配置文件

#### 1. Docker Compose配置

| 文件 | 位置 | 用途 | 说明 |
|------|------|------|------|
| `docker-compose.yml` | 项目根目录 | 基础配置 | 定义所有服务的基本配置（MySQL、后端、前端） |
| `docker-compose.dev.yml` | 项目根目录 | 开发环境配置 | 热重载、远程调试、源码挂载 |
| `docker-compose.prod.yml` | 项目根目录 | 生产环境配置 | 资源限制、日志轮转、多副本 |

**使用方式**:
```bash
# 默认模式
docker-compose up -d

# 开发模式
docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d

# 生产模式
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

#### 2. Dockerfile配置

| 文件 | 位置 | 用途 | 构建方式 |
|------|------|------|----------|
| `smartmes-backend/Dockerfile` | 后端目录 | 生产环境后端镜像 | 多阶段构建（Maven + JRE） |
| `smartmes-backend/Dockerfile.dev` | 后端目录 | 开发环境后端镜像 | Spring Boot热重载 |
| `smartmes-frontend/Dockerfile` | 前端目录 | 生产环境前端镜像 | 多阶段构建（Node + Nginx） |
| `smartmes-frontend/Dockerfile.dev` | 前端目录 | 开发环境前端镜像 | Vite开发服务器 |

**特性**:
- **生产Dockerfile**:
  - 多阶段构建减小镜像体积
  - 非root用户运行提高安全性
  - 内置健康检查

- **开发Dockerfile**:
  - 支持热重载
  - 挂载源码目录
  - 开启调试端口

#### 3. 环境配置文件

| 文件 | 位置 | 用途 | 说明 |
|------|------|------|------|
| `.env.example` | 项目根目录 | 环境变量模板 | 包含所有可配置项和默认值 |
| `.dockerignore` | 项目根目录 | Docker忽略文件 | 排除不需要的文件 |
| `smartmes-backend/.dockerignore` | 后端目录 | 后端忽略文件 | Maven、IDE文件等 |
| `smartmes-frontend/.dockerignore` | 前端目录 | 前端忽略文件 | node_modules、构建产物等 |

**配置步骤**:
```bash
# 1. 复制模板
cp .env.example .env

# 2. 编辑配置
nano .env

# 3. 启动服务
./start.sh
```

### 服务配置文件

#### 4. Nginx配置

| 文件 | 位置 | 用途 | 功能 |
|------|------|------|------|
| `smartmes-frontend/nginx.conf` | 前端目录 | Nginx服务器配置 | 静态托管、反向代理、压缩、缓存 |

**主要功能**:
- 静态文件托管（Vue应用）
- API反向代理到后端（/api -> backend:8080）
- Gzip压缩
- 静态资源缓存
- Vue Router历史模式支持
- 安全头部配置

#### 5. MySQL配置

| 文件 | 位置 | 用途 | 功能 |
|------|------|------|------|
| `database/my.cnf` | 数据库目录 | MySQL配置文件 | 字符集、性能优化、日志配置 |

**优化项**:
- UTF-8字符集
- InnoDB缓冲池优化
- 连接数配置
- 慢查询日志
- 二进制日志

### 脚本文件

#### 6. 启动和管理脚本

| 文件 | 位置 | 用途 | 功能 |
|------|------|------|------|
| `start.sh` | 项目根目录 | 一键启动脚本 | 检查Docker、启动服务、初始化数据库 |
| `stop.sh` | 项目根目录 | 停止服务脚本 | 优雅停止所有服务 |
| `database/init-db.sh` | 数据库目录 | 数据库初始化脚本 | 等待MySQL就绪、执行初始化SQL |
| `Makefile` | 项目根目录 | Make命令集合 | 提供便捷的管理命令 |

**脚本特性**:
- 带颜色的输出信息
- 完整的错误处理
- 健康检查
- 详细的日志输出

### 文档文件

#### 7. 文档资源

| 文件 | 位置 | 用途 | 内容 |
|------|------|------|------|
| `README.md` | 项目根目录 | 主要文档 | 项目介绍、安装、使用、API文档 |
| `DOCKER.md` | 项目根目录 | Docker详细指南 | Docker配置说明、故障排查 |
| `QUICK_START.md` | 项目根目录 | 快速开始指南 | 5分钟快速部署 |
| `.gitignore` | 项目根目录 | Git忽略文件 | 版本控制排除项 |

## 配置文件关系图

```
test_demo/
│
├── Docker Compose层
│   ├── docker-compose.yml (基础)
│   ├── docker-compose.dev.yml (开发)
│   └── docker-compose.prod.yml (生产)
│
├── 后端服务
│   ├── smartmes-backend/
│   │   ├── Dockerfile (生产)
│   │   ├── Dockerfile.dev (开发)
│   │   └── .dockerignore
│   └── application.yml (Spring配置)
│
├── 前端服务
│   ├── smartmes-frontend/
│   │   ├── Dockerfile (生产)
│   │   ├── Dockerfile.dev (开发)
│   │   ├── nginx.conf
│   │   └── .dockerignore
│   └── .env.example
│
├── 数据库服务
│   └── database/
│       ├── my.cnf (MySQL配置)
│       ├── schema.sql (表结构)
│       ├── init_data.sql (初始数据)
│       └── init-db.sh (初始化脚本)
│
├── 管理脚本
│   ├── start.sh (启动)
│   ├── stop.sh (停止)
│   └── Makefile (便捷命令)
│
└── 配置文件
    ├── .env.example (环境变量模板)
    ├── .dockerignore (Docker忽略)
    └── .gitignore (Git忽略)
```

## 启动流程

### 1. 默认模式启动

```bash
./start.sh
```

执行流程：
1. ✅ 检查Docker是否运行
2. ✅ 检查Docker Compose
3. ✅ 创建必要的目录
4. ✅ 复制.env文件
5. ✅ 启动docker-compose.yml
6. ✅ 等待MySQL就绪
7. ✅ 自动初始化数据库
8. ✅ 显示访问信息

### 2. 开发模式启动

```bash
./start.sh dev
```

使用配置：
- `docker-compose.yml` (基础)
- `docker-compose.dev.yml` (开发增强)

特性：
- 源码热重载
- 远程调试端口
- 开发服务器

### 3. 生产模式启动

```bash
./start.sh prod
```

使用配置：
- `docker-compose.yml` (基础)
- `docker-compose.prod.yml` (生产优化)

特性：
- 资源限制
- 日志轮转
- 多副本支持

## 环境变量配置

### 主要环境变量

在`.env`文件中配置：

```bash
# 数据库配置
MYSQL_ROOT_PASSWORD=smartmes123
MYSQL_DATABASE=smartmes
MYSQL_USER=smartmes
MYSQL_PASSWORD=smartmes123

# 端口配置
BACKEND_PORT=8080
FRONTEND_PORT=80
MYSQL_PORT=3306

# Spring配置
SPRING_PROFILES_ACTIVE=prod
JAVA_OPTS=-Xms512m -Xmx1024m

# 安全配置
JWT_SECRET=your-secret-key
JWT_EXPIRATION=24
```

### 配置优先级

1. `.env` 文件（最高优先级）
2. `docker-compose.yml` 中的默认值
3. Dockerfile中的ENV指令

## 数据持久化

### 数据卷配置

在`docker-compose.yml`中定义：

```yaml
volumes:
  mysql-data:        # MySQL数据
  backend-logs:      # 后端日志
  node-modules:      # npm依赖（开发模式）
  maven-cache:       # Maven缓存（开发模式）
```

### 数据位置

| 数据类型 | 容器内路径 | 宿主机位置 | 说明 |
|---------|-----------|-----------|------|
| MySQL数据 | `/var/lib/mysql` | Docker卷 | 持久化数据库 |
| 后端日志 | `/app/logs` | `./logs/backend` | 应用日志 |
| Nginx日志 | `/var/log/nginx` | 容器内 | Web服务器日志 |
| 数据备份 | - | `./backups` | 手动备份 |

## 网络架构

### 网络配置

```yaml
networks:
  smartmes-network:
    driver: bridge
```

### 服务通信

```
前端容器 (frontend:80)
    ↓ Nginx反向代理
后端容器 (backend:8080)
    ↓ JDBC连接
数据库容器 (mysql:3306)
```

### 端口映射

| 服务 | 容器端口 | 宿主机端口 | 说明 |
|------|---------|-----------|------|
| 前端 | 80 | 80 | HTTP访问 |
| 后端 | 8080 | 8080 | API访问 |
| MySQL | 3306 | 3306 | 数据库连接 |
| 后端调试 | 5005 | 5005 | 远程调试（开发模式） |
| Vite开发 | 5173 | 5173 | 前端开发服务器（开发模式） |

## 健康检查配置

### MySQL健康检查

```yaml
healthcheck:
  test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
  interval: 10s
  timeout: 5s
  retries: 5
  start_period: 30s
```

### 后端健康检查

```yaml
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 60s
```

### 前端健康检查

```yaml
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:80"]
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 10s
```

## 快速命令参考

### 使用start.sh

```bash
./start.sh           # 默认模式启动
./start.sh dev       # 开发模式启动
./start.sh prod      # 生产模式启动
./start.sh clean     # 清理所有
./start.sh --help    # 查看帮助
```

### 使用Makefile

```bash
make help            # 查看所有命令
make up              # 启动服务
make dev             # 开发模式
make prod            # 生产模式
make down            # 停止服务
make logs            # 查看日志
make ps              # 服务状态
make backup          # 备份数据库
make restore         # 恢复数据库
make clean           # 清理所有
```

### 使用docker-compose

```bash
# 启动服务
docker-compose up -d

# 查看状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 进入容器
docker-compose exec backend bash
```

## 故障排查

### 查看日志

```bash
# 所有服务日志
docker-compose logs -f

# 特定服务日志
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f mysql

# 最近100行日志
docker-compose logs --tail=100 backend
```

### 检查服务状态

```bash
# 查看所有服务
docker-compose ps

# 查看资源使用
docker stats

# 查看健康状态
docker inspect smartmes-backend | grep -A 5 Health
```

### 重建服务

```bash
# 重建特定服务
docker-compose up -d --build backend

# 强制重建
docker-compose up -d --force-recreate backend

# 重建所有服务
docker-compose down
docker-compose up -d --build
```

## 最佳实践

### 1. 配置管理
- ✅ 使用`.env`文件管理配置
- ✅ 不要提交`.env`到版本控制
- ✅ 生产环境修改默认密码

### 2. 数据备份
- ✅ 定期备份数据库
- ✅ 测试恢复流程
- ✅ 使用`make backup`命令

### 3. 日志管理
- ✅ 配置日志轮转
- ✅ 定期清理旧日志
- ✅ 监控日志大小

### 4. 安全措施
- ✅ 修改默认密码
- ✅ 使用强JWT密钥
- ✅ 限制端口暴露
- ✅ 非root用户运行容器

### 5. 性能优化
- ✅ 生产环境使用资源限制
- ✅ 配置合适的JVM参数
- ✅ 启用Nginx缓存和压缩

## 总结

本项目提供了完整的Docker容器化配置，包括：

- ✅ 3个Docker Compose配置文件（基础、开发、生产）
- ✅ 4个Dockerfile（后端生产/开发、前端生产/开发）
- ✅ 完整的环境配置文件
- ✅ 自动化启动和管理脚本
- ✅ 详细的文档说明
- ✅ 便捷的Makefile命令

所有配置文件都包含详细的中文注释，易于理解和维护。

## 快速链接

- 📖 [主要文档](README.md)
- 🚀 [快速开始](QUICK_START.md)
- 🐳 [Docker详细指南](DOCKER.md)
- 🗄️ [数据库文档](database/README.md)
- 💻 [后端文档](smartmes-backend/README.md)
- 🎨 [前端文档](smartmes-frontend/README.md)

---

**SmartMES Lite** - 完整的Docker容器化解决方案
